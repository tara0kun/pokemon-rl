# Virtual environment (869MB)
poke-rl/
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# Game ROM (copyrighted — NEVER commit)
*.gba
*.gb
*.gbc
*.nds

# Save states (in-game saves — copyright + user data)
*.sav
*.srm
*.ss*
saved_states/

# Screenshots (46MB recording history)
screenshots/

# Training logs (per-run, regenerable)
training_current.log
training_*.log
logs/
*.log

# Tensorboard
events.out.tfevents.*

# Backup files
*.bak
*.bak_*
exploration_map.json.bak_*
wall_hits.json.bak_*

# Secrets (defensive — should never be in repo)
.env
.env.local
*api_key*
*ANTHROPIC*
.claude/settings.local.json

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Temp / cache
/tmp/
*.tmp
*.cache

# Large model artifacts (optional — keep small representative samples)
models/*.zip
!models/README.md

# Canon data binary cache (regenerable from pokeemerald)
docs/canon_data_*.bin

# Tile collection data (49,950 PNG tiles — too large for git)
tile_data/

# wall_hits backups (regenerable)
wall_hits_backup_*.json

# .spc_history (per-port stuck pos cache, regenerable)
.spc_history.json

# Claude Code per-project config (settings.local.json + agents.json)
.claude/

# state load log (historical)
state_load_log.md

# archive/ (old docs, very large)
archive/

# Private AI session context (not for public)
memory/

# Daily dev journal (private)
daily_progress/

# Runtime save metadata (per-port, regenerable)
slot4_meta_*.json

# exploration_map backups (regenerable, large)
exploration_map_backup_*.json

# restart loop script (env-specific)
restart_loop.sh

# Internal docs (rules/violations/risks — not for portfolio)
docs/RULE_VIOLATIONS.md
docs/MONITORING_GUIDE.md
docs/KNOWN_RISKS.md
docs/UNRESOLVED_ISSUES.md
docs/USER_GUIDE.md
PROGRESS.md

# Legacy project — moved 2026-06-08, retained for reference
# (commented out; user wants to track legacy for portfolio history)
# legacy/

# Anthropic CLI (downloaded binary, regenerable)
ant/

# generic_agent runtime artifacts
generic_agent/logs/
generic_agent/memory/notes.jsonl
generic_agent/memory/run_log.jsonl
generic_agent/memory/frame_cache.json
generic_agent/__pycache__/
generic_agent/rom/*.gba
generic_agent/rom/*.sav
