# 2026-07-10 — KNUCKLE BADGE (Badge 2) 自律取得

## 結果

Grovyle **L26 → L30**、L29 で **Leaf Blade**(move 348, 70 power Grass)習得 → **Brawly 撃破 → Knuckle Badge (badge_count=2)**。撃破時 HP 2/86 = 辛勝。全て claude_heuristic の自律走行(saveStateLoad / RAM 書込なし)。

## 真因は「grind 場所」ではなく「稼働ループの取り違え」だった

前セッションまで grind が永遠に収束しなかった真の理由:

- **`auto_loop` は goals も battle_moves も import せず、野生戦を "Down,Right,A" で逃走する別ループ**(auto_loop.py L385-403）。goal 誘導ゼロ、frontier 探索のみ。→ 野生戦から毎回逃げるのでレベルが上がりようがなかった。
- ストーリーを進める本体は **`claude_heuristic`**(`heuristic_button(current_goal=...)` を主決定に使い、goals.py / battle_moves.py / map_data の warp routing を駆動）。今回これを正しく起動して初めて grind が機能した。
- CLAUDE.md の STARTUP が auto_loop を記載していたため誤起動していた。memory(current_project.md）に「稼働ループの区別」を最重要として追記済み。

## Fable 二AIクロスチェックの成果

自分のソロ推奨(Route106 (65,16) にピン)を Fable が一次データで棄却:
- pokeemerald `wild_encounters.json` に **Route106 の `land_mons` が無い**(水/釣りのみ）。agent 自身の map cache も `grass_tiles: []`。→ Route106 ピンはエンカウント 0 で無意味だった。
- 正解 = **Granite Cave 1F**(全床タイルでエンカウント、`requires_flash: False` で明るい。暗いのは B1F のみ）。
- Grovyle は **L29 で Leaf Blade** 習得 → L30 で Brawly を薙げる、という Fable の指摘も実機で確認。

二つの AI は同じ結論に**ならず**、Fable が私の致命的誤り(場所ゼロ地点)を潰した。これが「二つのAIで開発」の実利。

## 実装(commit b96f2cd11, dev）

- `goals.py`: grind 目標を Route106 → **Granite Cave 1F (24,7) target_pos (27,7)**(全 warp/梯子から 12 タイル）に retarget。`grind_route106` → `grind_granite_cave` に rename。grind/heal 条件に B1F (24,8) を自己修復ガードとして追加。
- `claude_heuristic.py`: wild 分岐に **`enemy_hp==0 → ["A"]`**（任意 HP/ball 状態）。L29 の Leaf Blade 習得プロンプトを A 連打で確定(move_select_sequence の先頭 B,B が習得をキャンセルし、Fallarbor の Move Relearner 到達不能 = 永久ロスになるのを防ぐ）。→ 実機で Leaf Blade 習得を確認(slot0 Pound→348）。
- テスト 83 件 green。

## 走行の実態

- Granite Cave 1F (27,7) pin で interact-oscillation が pacing → 全床タイルでエンカウント → best_move で戦闘（Leaf Blade / Pursuit / Quick Attack を PP・タイプ相性で選択、0-PP スキップ済み）。
- L30 到達で grind goal 解除 → `reach_dewford_gym`(cave 退出）→ `dewford_gym_brawly`(4,3）へ遷移し Brawly 戦。全遷移をオフラインでも検証済み。
- 運用: `scratch/grind_supervisor.py`（1500 turn チャンク × 再起動、chunk 間で level/moves/badge を安全 read、badge==2 で自動停止）。実測 2 チャンク(約 2 時間）で badge。

## 次

- Slateport 方面の goal chain(docs/HYPOTHESES.md H4）は未実装。
- 任意の最適化: grind gate を `L>=30 OR (L>=29 AND Leaf Blade 所持)` にすると ~750 turn 短縮（move-id を GameState に通す配線が要る）。
- 検討: CLAUDE.md STARTUP と auto_loop / claude_heuristic の役割整理（VLM pivot と実稼働ループの乖離）。user 判断待ち。
