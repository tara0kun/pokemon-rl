# 2026-07-06 daily progress

大きく2トラック: (A) ゲーム進行 — Peeko 救出後〜Dewford 航海完遂、
(B) dual_dev (Claude + Codex/SakanaAI 二体制) の quota-safe 24/7 基盤を実装・実測。

---

# Track A — Peeko 救出後 → Dewford 航海完遂

## マイルストーン
- **Dewford Town 到達・定着**(Stone Badge 保持のまま。次は Brawly / Badge 3)
- Treecko → Grovyle は前セッションで進化済み

## SaveBlock1 ポインタ安定化 (commit 9998ad652)
- 症状: 戦闘中に `FLAG_RECOVERED_DEVON_GOODS` (0x8F) が 6回中1回 False に flicker
- 真因: Emerald は SaveBlock1 を毎フレーム DMA 再配置。gSaveBlock1Ptr を再配置中に
  読むと「範囲内だが過渡アドレス」を掴み、その先の flag 読みが 0 を返す
- 修正: `_read_saveblock1_ptr()` — 連続2回一致 + EWRAM 範囲の pointer のみ採用
- [dual_dev cycle1 手動モードで設計→Codex実装→レビュー→commit した最初の実運用例]

## peeko_done を disk latch 化 (commit bbb634b4b)
- flag flicker で goal が rescue_peeko ↔ peeko_return 振動 → 一度 True を見たら
  `peeko_done.marker` に永続 latch。以降 flag=False でも peeko_return を選択

## Dewford 出航メカニクスの完全解明
2段階の warp + 徘徊 NPC:
1. house の **Mr.Briney (EXPERT_M)** に話す → "Anchors aweigh" → Route104 桟橋 (13,51) に warp
2. 桟橋で A → 航海 (Route105 → Route106 → DewfordTown)
- **Peeko 救出後 (`VAR_BRINEY_HOUSE_STATE=1`)、Briney と Peeko は house 内を徘徊移動**
  (canon の固定 (5,3)/(6,3) ではない)。RAM の NPC 位置読みも間欠的で mid-step 座標を返す
- **到着トラップ**: Dewford 到着時 player は桟橋の Briney に隣接配置され、A を押すと
  帰りの渡し船が起動 → Route104 に戻ってしまう。到着後は桟橋から離れてから操作する

## 未解決 / 次のギャップ
- **徘徊 NPC への確実な話しかけ** = agent のナビ実装のギャップ。自動化を3回試みたが
  (roam sweep×2, NPC chase×1)、徘徊 + 間欠 read で安定収束せず。最終ステップは user 手動で突破
- in-battle flicker の実機確認は航海に野生戦がなく未消化 → 次の野生戦で devon 20回サンプル

---

# Track B — dual_dev quota-safe 24/7 基盤

## モデル運用方針
- 普段 Opus 4.8 / 難問のみ Fable (コスト)。`~/.claude/settings.json`:
  model=opus, availableModels=[opus,fable]

## 実装した基盤 (全て commit + push 済み)
| commit | 内容 |
|--------|------|
| 14adc70ae | 永続 rate limiter — Codex 呼び出しを最小間隔で throttle (セッション跨ぎ・crash-safe)。Win32 OpenProcess で liveness 判定 |
| 15c923e52 | task backlog queue — `--enqueue` / `--from-queue`、空で停止 |
| 7abe29dba | 自動タスク生成器 (option A) — Claude がコード分析して小タスク補充 (**Codex 消費ゼロ**)。`--generate-tasks` / `--auto-refill` |
| 78b5479b8 | サイクル分離 (revert) — commit しないサイクルの tree 変更を戻す |
| cc9fd1f92 | サイクル分離 (baseline advance) — gate baseline を毎サイクル現ツリーへ前進 |
| 2cf2a0d5c | stale-run reaper — 死んだ orchestrate run を abort (Codex 無限待機を防ぐ) |

## watch-run 検証 (実機)
- 3サイクル全 PASS 自動 commit (preprocess/memory/io の実改善: 1becdaa9d / 6e4db34ad / 7e7f0a8ce)
- demo が「サイクル間の累積 diff がゲート違反」バグを炙り出し → 分離修正 (revert + baseline) で解決

## quota 実測 (重要)
SakanaAI ダッシュボードで判明した真の制約:
- **5時間ローリング枠**が拘束(今朝の集中実行で 100% → 数時間で自動リセット)
- **週間枠**は余裕(35%)
- fugu は high reasoning で1呼び出しのトークンが非常に重い
- **方針確定**: 終日・超低レート(`--codex-min-interval 14400` = 4時間間隔)。5時間枠を
  バーストせず、user の対話 Codex 利用と quota を共有。ループは自動 pause の安全網あり

## 運用上の教訓
- **全 python 一括 kill 禁止**: dual_dev orchestrate を巻き添えにすると run-state が
  running のまま固着し Codex 側が無限待機。claude_heuristic だけを command line で絞って kill
- 終日ループ (run_20260706_112639) は detached で起動。初 Codex 呼び出しは 14:40 に設定
  (5時間枠リセット後)。git 整理のため一時停止 → 整理後に resume 予定

---

# Track C — その他の修正
- **低HP whiteout ガード** (claude_heuristic.py): 野生戦で party0 が critical HP (<=25%) の時、
  FIGHT/catch でなく RUN を選び whiteout (全滅→強制 warp + 所持金半減) を回避。トレーナー戦は除外
- dual_dev Track B hardening (README/clients/config): 前セッションの Codex 実装分を commit

---

# Track D — H1(Dewford Gym ドア振動)完全解決 + Opus×Fable 二体制の試運転

**文脈**: 別窓の Fable で「Fable 5 提供終了に備えた capability banking」(知識パック docs/ + 決定的テスト 49件 + CI)を整備済み。本セッションは **その足場を使って Opus がこの問題を解けるか + Fable と比較** する試運転。

## H1 root cause 確定(2 バグ複合)— docs/HYPOTHESES.md H1 に詳細
1. **`warp_step_direction` の中点ヒューリスティックが下半分の建物ドアで誤判定** → door タイル (8,17) で "Down" を返し後退させる。全 248 マップ・490 interior warp 監査で **127 warp が誤り(曖昧さゼロ)**。collision 由来(歩ける接近側→塞がれた側)ルールに置換。
2. **非歩行 door タイル上で goal nav ブロックが walkable ゲートにより沈黙** → 「もう一度 Up」が出ず wander でドアから降りる。`mapbfs_warp_on` 分岐を追加。

## 修正 + 検証
- `map_data.py` warp_step_direction / `claude_heuristic.py:394` 直前に mid-door-entry 分岐 / `tests/test_map_data.py`(合成 MapInfo・ネットワーク不要の 6 件)。**計 55 テスト PASS**。
- **制御タップ実測**: (8,19)→Up→(8,18)→Up→(8,17)→Up→**map 3-3 warp**。
- **live 走行**: エージェントが Gym 迷路を突破し **Brawly (4,3) に到達・戦闘起動**(ドア振動は再現せず)= H1 完全解決。

## Opus×Fable 比較(user 依頼「必要なら fable を使って比較」)
- Opus の診断を伏せて Fable に同じ証拠から独立診断させたところ **2 バグとも完全収束**。
- Fable が全マップ監査を提案(当初 62 flip 報告)→ Opus が独立再現し **127 flip に拡大確認**。collision 由来ルールへの一般化は Fable 案を採用。**二体制は狙い通り機能**(独立収束 → 相互に強化)。

## 副産物: バトルAIの弱点表面化(H6・別問題)
- H1 の live 走行で Brawly 戦に入ったが **Grovyle L24 が opening のメニュー取り違えで反撃前に気絶** → 控え L3-L7 では詰み → whiteout 不可避。
- 真因(decisions 実測): opening turns 8-16 が交代用 `party_seq` に落ちて空打ち(H6a)。控えが低レベル(H6b)。
- **user 判断: H1 完了を優先し一旦区切る。バトルAI(H6)は別セッション。**
- ゲーム状態: Brawly 戦で敗北確定のため停止。復帰は **savestate_dewford.ss1**(クリーン Dewford・健全 Grovyle・ストーリー進行ゼロ)から。setback なし。

## 運用メモ
- **走行中の socket は 1 接続前提**: 診断で別 `read_state` を張るとループの読みと衝突し corrupt read((0,0)/species 誤り)を誘発。監視は decisions ログ(ファイル)のみで行い、socket 直読みはループ停止後にする。
- claude_heuristic は venv ランチャで python.exe が親+子の 2 プロセスに見えるが正常(競合ではない)。kill は commandline 絞りで両方まとめて。
