# 2026-06-12 daily progress (cycles 21-24)

## TL;DR

Option X (位置認識 path_memory) を実装、 cycle 23/24 として deploy。
mechanical には機能 (v44 の (1, 3) 1408 turn stuck → v46 で 4 turn に劇減)、
だが coverage は v43 (cycle 14-21 fresh state) の 261 pos を超えず、
構造的に Route 101 を抜けない問題が残る。 v43 が事実上の安定 best。

## 本日の run trend

| run | cycle | unique pos | maps | cost | 備考 |
|-----|-------|------------|------|------|------|
| v43 | 14-21 fresh state | **261** | 4 | $1.50 | Littleroot 徹底探索 |
| v44 | 22 full GOAL_DIRECTION | 44 | 4 | $1.50 | (1,3) で 1408 turn stuck |
| v45 | 22 conditional (>=100) | 184 | 5 | $1.50 | (1,2) で 517 turn stuck |
| v46 | 23 position-aware | 134 | 5 | $1.50 | (1,3) stuck 4 turn = 改善 |
| v47 | 23+24 onward-score | 204 | 4 | $1.50 | GOAL_DIRECTION 0 fire |

best = **v43 261 pos**、 これは cycle 14-21 + fresh memory 構成。

---

## §29 Cycle 21: clean_memory routine (tools/)

### 動機
v42 (cycle 14-20、 累積 state) = 79 pos、 v43 (同コード、 wipe 後) = 261 pos。
**stale persistent state が真の bottleneck** だった。 cycle 19 の cleanup_phantom_walls
は tile_map だけ、 cache + path_memory はそのまま。 都度 PowerShell で wipe
するのは脆い → tool 化。

### 実装
`generic_agent/tools/clean_memory.py`: frame_cache.json / tile_map.json /
path_memory.json を timestamped `.bak` バックアップして移動。 `--yes` skip 確認。

### 教訓
コードは正しいのに退化していたのは architectural な「過剰防御の累積」 + 「state
poison の累積」 の両方が原因。 後者が支配的だったことを v42→v43 比較で証明。

---

## §30 Cycle 22 conditional (v45 で deploy)

### 動機
v34 best 279 pos / 8 maps は path_memory に北側 transition データを持って
いた v33 state を受け継いでいたから可能だった。 fresh state では Brain が
「どちらが北か」 を知る情報源無し。 GOAL_DIRECTION を data-driven に注入する。

### 実装
- path_memory に candidate target_map で visits 最少 (= under-explored)
  を選ぶ
- first step を `parts.append(f"GOAL_DIRECTION={gdir} ...")` で prompt 注入
- SYSTEM_PROMPT_NAVIGATE_LONG HARD PRIORITY rule #3 として 「GOAL_DIRECTION
  must pick」 追加

### v44 結果 (unconditional)
85 fire、 (0, 9) Littleroot Up → (1, 3) May 2F に push → 1408 turn stuck。
全 budget の 94% を 1 部屋に消費。 44 pos / $1.50 で **過去最悪 regression**。

### v45 結果 (conditional same_map_streak >= 100)
35 fire、 first fire turn 264。 (1, 2) で 517 turn stuck だが (1, 3) は回避。
184 pos / 5 maps。 v44 から回復したが v43 を超えず。

### 根本問題発覚
path_memory が **position-blind**: (0, 9) で「Up を押すと 0-16」 が 5 つの
異なる出口 tile で全部 fires。 Brain が screenshot で区別できる情報なのに、
GOAL_DIRECTION が forcing するため visual context override 不能。 → Cycle 23 へ。

---

## §31 Cycle 23: position-aware path_memory

### 実装 (path_memory.py 大幅改修)

#### 旧 schema
```json
{ "0-9": { "0-16": [["Up","Up","Up","Up","Up","Up","Up","Up"]] } }
```

#### 新 schema
```json
{
  "0-9": {
    "0-16": [{
      "from_pos": [11, 0],
      "to_pos": [11, 19],
      "seq": ["Up","Up","Up","Up","Up","Up","Up","Up"]
    }],
    "1-2": [{
      "from_pos": [14, 8],
      "to_pos": [2, 8],
      "seq": ["Up","Up","Up","Up","Up","Up","Up","Up"]
    }]
  }
}
```

同 `from_map → to_map` で `from_pos` が異なる record を **重複保存**。 Brain は
agent の cur_pos に Manhattan 距離 ≤5 の record のみ表示される。

#### 後方互換
旧 schema (list of seq) は `from_pos=None` の record として load。 distance
filter で自然に排除されるので新コードは旧 .json を読めるが旧 record は使われない。

#### nearby_records() API
新規メソッド: `(cur_g, cur_n, cur_x, cur_y, max_distance=5)` → `[(target_key,
record, distance), ...]` sorted by distance。

### v46 結果
- 134 pos / 5 maps / $1.50
- **(1, 3) 致命的 stuck 解消**: v44 1408 turn → 4 turn
- 0-9 から 1-2, 0-16, 1-4 を異なる from_pos で記録、 (5,5) と (7,8) と (12,7)
  でそれぞれ別の hint が出る (unit test 確認済)
- だが GOAL_DIRECTION が dead-end map (1, 3) を picks → 進歩方向選べず

### Mechanical success, statistical inconclusive
position-aware の効果は path_memory.json に直接観測可能、 (1, 3) 回避は v44 比較で明らか。 ただし coverage は v43 261 → v46 134 で regression。 GOAL_DIRECTION の選好ロジックが naive。

---

## §32 Cycle 24: onward-score weighting

### 動機
GOAL_DIRECTION が「visits 最少」 で選ぶと dead-end indoor (1, 3 は
1-2 にしか戻れない) が「未訪問だから魅力的」 と判定される。 本当に欲しいのは
「先に新しい場所へ進める map」。

### 実装
各 candidate target で `onward_score = self を除いた transition 数`:
- (1, 3) onward_score = 0 (1-2 にしか戻れない、 self loop)
- (0, 9) onward_score = 3+ (1-2, 1-4, 0-16, ...)

candidates ソートを `(-onward_score, t_visits, dist)` に変更 → dead-end 回避。

### v47 結果
- 204 pos / 4 maps / $1.50
- **GOAL_DIRECTION 0 fire** (max same_map_streak 74 < 100 閾値)
- onward_score 機構の本番 fire 観測できず = 検証 inconclusive
- 偶然 v43 と似た「自由探索」 状態で 204 pos = v43 261 にわずか劣る

---

## 構造的に判明したこと

v43, v46, v47 すべて **(0, 16) Route 101 = 21-24 turn しか滞在しない**。

### 失敗連鎖
1. agent が南端から Route 101 に入る
2. tile_map ほぼ空 → BFS が全方向 frontier 認識
3. BFS prefer='nearest' = entry tile 近傍を picks → 南端方向
4. 南端方向は Littleroot 国境 → 24 turn で戻る
5. MapSurveyor は stale_turns=20 で exit → surveyed_this_session.add(0-16)
6. 二度目以降 Route 101 で surveyor は再 trigger されない

### Cycle 25 候補
- BFS prefer='farthest' を fresh map (visit count < N) で default 化
- MapSurveyor の surveyed_this_session 解除条件 (例: tile_map のタイル数 <
  target/3 なら re-fire 許可)
- map 入場直後 K turn は「逆方向 (= 入った方向の逆)」 を強 bias

---

## Branch / commit (本日)

- `dev`: cycle 21 (`cae1164`)、 cycle 22 conditional (`fc7ffaf`)、 cycle 23+24 (`4ecd927`)
- `main`: 未更新 (v0.1-portfolio-7cycles のまま)

### 暫定 milestone 候補
- **v43 構成 = cycle 14-21 + memory wipe**: 261 pos / 4 maps を fresh state で
  再現可能。 portfolio milestone tag `v0.2-fresh-state-261pos` の候補。
  ただし「8 maps 到達した v34 単一実例」 と引き替えに「再現性」 を取った構成。

### Strategic option (user 判断)
- A. Cycle 25 (BFS farthest on fresh map) を試行 → 北進開けるか検証
- B. v43 構成で main merge + milestone tag、 Pokemon RL portfolio として固定
- C. 別の architectural pivot (multi-frame vision、 task-budget 拡大 等)

私の素直な意見: **A を 1 回試して** 改善あれば main merge、 無ければ B で締める。
A は ~30 分の追加実装 + 1 run ($1.50) で済む程度の投資。
