# 2026-06-18 daily progress — Rival waypoint インフラ整備 + chronic encounter trap

## TL;DR
- 旧 rule-based (`old` branch) は完全分離維持。 今日は VLM agent (`generic_agent/`) 一本。
- `subprocess heur_rc=-1 timeout` 問題を 4 修正で解消、 連続 30+ iter で `heur_rc=0` 安定走行確立。
- canonical Rival waypoint (`find_npc_by_script_keyword`) + 段階的 fix で agent を Route 103 ((10,3)) へ収束させる枠組み完成。
- 一方で **Route 103 wild encounter 密度** が高すぎ、 Treecko Lv9-10 でも HP 削り切られて whiteout 反復、 unique_maps 22 で停滞。
- val_run score 83 plateau 継続 (iter 327+)。 CNN は heuristic demo を真似ているが demo 自体が stuck pattern。
- mGBA が午後 unexpectedly 終了、 session 中盤に手動 reconnect + `curriculum_savestates/milestone_m2_0_pos7_8.ss1` で Lv9 state 復元 (user 許可 emergency restore のみ paradigm)。

## commits (dev) — 11 件

| sha | 修正点 | 効果 |
|---|---|---|
| `488fe93` | screen `battle_menu` を `gs.in_battle` AND 条件から外す | RAM in_battle false negative でも battle 中の A 押下が機能 |
| `31b76f2` | feat: map_data.object_events 抽出 + `find_npc_by_script_keyword` + heuristic `rival_seek` policy | canonical Rival 座標を pokeemerald から runtime 取得 (ハードコード規則違反なし) |
| `cf6ad4c` | `rival_seek` priority を `hidden_battle_probe` より上に | iter 292 で agent が (11,14) で 500 turn stuck → 解消 |
| `0463a23` | feat: pre-Pokedex goal cascade (`reach_route_103_rival`) + skip-if-at-target | 取り出し flags<200 で Route 103 target を自動選択 |
| `e938fee` | tool: `rival_escort.py` one-shot Route 103 Rival bridge | (今 session では Treecko 重傷で whiteout 多発、 不安定) |
| `68faa27` | `io.load_state_file` 追加 (emergency restore only docstring) | mGBA unexpected exit 時の復元手段。 通常 saveStateLoad は禁止 |
| `5ee70f0` | `rival_seek_pivot`: stuck で direction 回転 | BFS 同方向ループ防止 |
| `528f155` | `rival_seek_pivot` DIRECTIONS gate 削除 | pivot 確実発火 (last_action='A' 後でも) |
| `24dff7f` | `battle_menu` check を `menu_visible` より先に | FIGHT menu を main menu と誤判定して B 連打 → 解消 |
| `ac11766` | pivot threshold 15 → 30、 rotation streak/10 | BFS が長く支配、 pure BFS で 1 tile/iter 進行確認 |
| `a055339` | pivot を walkable direction のみ選択 + last_action 回避 | wall 突き + ループ防止 |
| `0ac1f4c` | `rival_seek` goal-map gate (current map == goal target のみ発火) | Oldale で hidden Rival chase 抹消 |
| `bad5f6d` | goal-scan: any matching goal targets current map | cascade で reach_oldale 進む状態でも Route 103 Rival 認識 |
| `d95d2d9` | rival-name gate (goal name に "rival" 必須) | reach_oldale 不発化、 Oldale で mapbfs 通常運用 |
| `e7b6a25` | **anomaly_escape を goal-directed 決定で skip** | **critical override 修正**。 mapbfs/rival_seek の Up 判定が anomaly_escape の random Down 等で上書きされる現象を停止 (iter 358+ で適用) |

## 実測 plateau の理由 (e7b6a25 commit 前まで)
iter 327-356 (30 iter) の典型 decision histogram:
- `mapbfs`: 40-300
- `rival_seek` + `_pivot`: 200-700
- **`anomaly_escape`: 688-780** ← 700+/800 turn が anomaly により上書き

agent は 800 turn 中 700 turn ほど **random walk** していた。 これでは BFS の正解 path に乗らない。 mapbfs/rival_seek の Up を anomaly_escape の Down が常時上書きしていたため、 Oldale → Route 103 NORTH のはずが Route 101 SOUTH に流される現象が頻発。

## mGBA reconnect 経過
- 1500 ごろ mGBA process が消失 (理由不明)
- continuous_train wrapper は 18 iter (300-317) を空回り、 `new_demos=0` 連続 8 で early_stop
- ROM 再起動 + Lua reload (user 手動) で 1600 ごろ復活
- `savestate_recovery_lv9.ss1` (今日 1444 で手動 backup) で Lv9 27/27 state 復元
- v38 wrapper を `--max-iters 0 --train-threshold 300 --early-stop-after 10` で再起動
- 以降 iter 319 から走行、 現時点 iter 357 走行中

## memory 追記
- `project_pokedex_gate.md`: Painter gate (FLAG_ADVENTURE_STARTED = Pokedex 受領)、 sequence は Birch → Rival 戦 → Pokedex auto-trigger、 (20,10) gid=7 = Boy NPC 訴え混同に注意
- `feedback_running_shoes_stride.md`: Mom 取得後 1 press = 2 tile、 BFS step ごとに pos 再確認

## 次 session 課題
1. e7b6a25 適用後の挙動観察 (iter 358+)。 anomaly_escape 数百回 →数十回に下がるはず → mapbfs/rival_seek の指示通り Up に向かう
2. unique_maps 22 突破 (Route 102 / Petalburg) → score 83 plateau 突破
3. `FLAG_HIDE_ROUTE_103_RIVAL` clear 条件の特定 (現在 (10,3) Rival は visible 状態らしいが、 念のため確認)
4. in_battle RAM addr root cause 残課題 (`gBattleTypeFlags` 候補拡張)
